#pragma once

#include<iostream>
#include<ctime>

using namespace std;

#define MAX 100

enum MAINMENU {
	SIGNUP = 1,
	LOGIN = 2,
	ESCAPE = 3
};

enum LOGIN {
	RUNGAME = 1,
	LOGOUT = 2,
	DELETACC = 3
};